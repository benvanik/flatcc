namespace Eclectic;

table Buffer {
  data:[uint8];
}

enum Fruit : byte { Banana = -1, Orange = 42 }
table FooBar {
    meal      : Fruit = Banana;
    density   : long (deprecated);
    say       : string;
    height    : short;

    buffers:[Buffer];
}
file_identifier "NOOB";
root_type FooBar;
